language: c
dist: trusty
sudo: required
install: make
before_install: cd src && echo $PWD
compiler: gcc
jobs:
  include:
    - stage: make
      script:
        - echo ${TRAVIS_PULL_REQUEST_BRANCH:-$TRAVIS_BRANCH}
        - make clean
